---

# We're setting unused variables to false rather than simply not defining them
# to avoid this ugly situation https://github.com/ansible/ansible/issues/13274

TLS_CREATE_SELFSIGNED: false
TLS_PRIVKEY_SRC_FILE: false
TLS_CERT_SRC_FILE: false
TLS_CACHAIN_SRC_FILE: false
TLS_DEST_BASENAME: false
